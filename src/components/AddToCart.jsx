import { useState } from "react";

const AddToCart = () => {
  const [data, setData] = useState(null);

  return (
    <div>
      <ul>
        <li></li>
      </ul>
    </div>
  );
};

export default AddToCart;
